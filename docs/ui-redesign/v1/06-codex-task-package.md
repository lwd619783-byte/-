# Codex实施任务包｜Contract + Delta

## 派发状态

用户已于2026-09-09正式批准本V1.0成稿（APPROVED / FROZEN）。D0已按本票精确base核验并获派发，仅执行设计归档；本票不授权业务编码。D1–D5是完整任务规划，不是已经绑定未来base SHA的可执行提示词；前序合入后的SHA现在不能预测。不得把设计输入SHA复制成所有后续任务的假当前基线。

设计输入基线：`bcca135530352ed9c85407e18acceed1dbf45690`，来源为2026-09-09实时GitHub main读取。当前PR #38合入事实已可见，不在本包声称其CI全绿。[S01](09-source-register.md#s01主干状态)

### 模型和推理建议（不放入任务正文）

沿用用户当前客户端已可选的主力Codex模型：文档归档使用Medium，跨页面结构重排与状态隔离使用High，出现确实复杂的回归再提高。公开官方模型页本轮列出了GPT-5.6 Sol及不同推理档，但未验证到GPT-6条目；这不否定用户界面已显示的模型，也不据此猜测账号权限。不自动启用Ultra、多代理、实验功能或安装新Skill。[W05](09-source-register.md#w05官方codex模型与推理选择)

## D0｜设计归档与范围冻结（已批准并派发）

以下是本轮唯一绑定已核对完整SHA的票；运行时若main已变化，重新核对后重发，不自动升级到未知基线。

```text
仓库：lwd619783-byte/-。
精确 origin/main base：bcca135530352ed9c85407e18acceed1dbf45690。
前提：用户已明确批准 NEON-RC1-20260909；未批准则停止，不把“已生成”当“已冻结”。先fetch验证base；若不同，报告真实SHA与相关差异后停止。

目标：只把获批UI设计包归档为版本化事实源并建立执行索引，不写业务UI。
必读：根AGENTS.md、PRODUCT.md、docs/architecture.md、docs/feature-registry.md、UI workflow Skill，以及附件docs/01至07和design/index.json；长期规则只引用。

关键增量：①将获批docs/01至09完整归档到docs/ui-redesign/v1/并保留文件名，明确本次批准记录与设计输入SHA；②新增文档不得自封为数据合同最高优先级，不改写历史冻结文件；③图片是合成设计，不得移入真实数据目录，原A/B/C映射固定，C默认；④仅归档必要代表图和索引，不把全部重复图片塞入浏览器bundle。图片当前位置以附件实际解包路径为准，不猜本机目录。

验收：文档链接/图号/迁移56项对应、主题标识与状态一致；git diff仅含批准的设计文档/必要资产；沿用仓库适用门禁，未跑业务测试明确记NOT_RUN，不声称UI已实现。

分支codex/ui-v1-design-freeze；commit：docs: freeze approved research UI v1 design；普通push后停止。按仓库流程不建PR、不合并、不部署。交付HEAD、远端一致性、文件清单、校验与未执行项。
```

## D1｜共享外壳、三主题与状态保留

派发前：D0已独立审查并合入；重新获取精确origin/main并把SHA写入这张票。分支建议 `codex/ui-v1-theme-shell`。

目标：在既有React/Vite/Tailwind/Recharts基础上落地共享语义token、外观菜单、独立偏好与七入口外壳。必读获批01、03、04及layout/common目标代码和测试。

新增约束：主题只能改展示层，不改数据模式、图表类型、排序或Provider；切换不重建业务Store、丢表单或使焦点失踪；存储异常退回会话偏好且不触碰业务键；不随主题改字号、间距、DOM布局；无需新UI框架或大版本升级。

验收：三主题都可选且重开按有效偏好恢复；非法值、存储不可写、减少动态、键盘焦点有测试；在有筛选/选中公司/未提交内容时切换，业务导出前后保持一致。交付三主题同页截图与测试证据，检查现有门禁。commit `feat: add shared UI shell and switchable themes`，普通push后停止；审计通过前不PR/merge/deploy。

## D2｜个股池、快速预览与完整研究页

派发前：D1已审计合入并重获精确base。分支 `codex/ui-v1-company-research`。

目标：落实五标签完整公司研究、轻预览、可恢复导航和个股池基础/高级筛选。必读获批02 P04/P05、04、05 M13–M36，StockPool、StockDetailDrawer与相关测试。

新增约束：原详情区块与动作逐项有去向，不以折叠或视觉简化删功能；所有原数据/比较服务复用，官方只读和加载资格不变；标签/公司切换隔离迟到响应，不扩大首屏重数据加载；返回必须保留筛选、排序和位置，失效深链不能残留上一公司；仅重排已有研究文字，不新生成结论。

验收：56项映射中相关项逐项勾稽；五标签、原表/卡切换、预览关闭焦点、浏览器后退、长标题、无价格、港股财务未接入、明细失败可复现；三主题同布局及390px关键图；相关单元/集成、完整适用门禁、build与bundle检查。commit `feat: restructure company research views`；普通push后停止，交独立审计。

## D3｜首页、宏观与行业重排

派发前：D2已审计合入并刷新精确base。分支 `codex/ui-v1-research-overviews`。

目标：用同一壳层完成P01–P03，首页由大封面变工作台，宏观快照与来源分层，行业概览/细分/链条分离。必读获批02 P01–P03、03 C01/C03/C07/C08、05 M01–M12及目标服务/页面。

新增约束：不得把图中62分、行业景气分、新闻或指数序列作为真实功能；宏观历史只有当前消费路径有正式数据才显示，不能顺手跨接Node-only或source catalog；行业数量条明确研究池分母，保留机器人全部/未上市线索；首页提醒只依赖现有业务逻辑，不建新的AI或评分系统。

验收：现有指标与九分类完整可达，生成时间与发布时间未混；行业/细分返回上下文保留；所有模板与缺失状态清楚；八页共用theme结构无分叉；相关测试、UI审计、build与既有真实性门禁；截图含宏观无历史、行业缺资料和首页无观察项。commit `feat: redesign research overview pages`；普通push后停止。

## D4｜观察、验证、预期及复杂表单

派发前：D3已审计合入并刷新精确base。分支 `codex/ui-v1-evidence-workflows`。

目标：完成P06–P08与元数据/复盘/预期/导入/备份的统一视觉，保留现有业务意图。必读获批02 P06–P08、04、05 M37–M56和原Store/比较/完整性测试。

新增约束：确认提醒不等于改变判断，元数据与复盘边界不变；官方索引失败全局关闭和明细部分失败的差异继续传播；重复不双计、内容冲突仍阻断、官方记录不可纠改；日期精度/业务并列/修订与纠错不因时间线美化丢失；导入UI不能自行放宽validator或改变写入原子语义。

验收：原十类预期筛选与全部低频入口可达；故障夹具、旧记录导入/导出、复盘纠错与归档恢复、来源冲突、索引关闭和本地继续使用有回归；三主题不改变业务快照；相关全部集成、契约/数据审计适用检查及build。commit `feat: unify evidence and review workflows`；普通push后停止。

## D5｜全站视觉验收与边界修复

派发前：D4已审计合入并刷新精确base。分支 `codex/ui-v1-visual-acceptance`。

目标：按获批07做最终跨主题/窄屏/异常状态验收，仅修本改版引入的问题，不借收尾扩大功能。必读07、已有UI workflow与各票审计记录；Taste/Impeccable仅按仓库现有路由使用。

新增约束：截图不是业务测试；142配色对不是浏览器无障碍认证；只能按主题值差异解决视觉问题，不能让light/pro另有业务渲染分支；维持重数据lazy边界，不引入持续WebGL背景或全市场预取；测试失败不能降低门禁。

验收：24张桌面主页面截图（8×3）+8张霓虹窄屏+关键表单两种替代主题；320px/200%缩放、键盘与reduced-motion、主题切换状态保留、图表缺值和大负数；核对业务导出不因主题改变；提交完整适用测试/build/UI审计/性能和bundle前后证据，真实标注NOT_RUN与阻断。commit `fix: complete UI v1 cross-theme acceptance`；普通push后停止，交ChatGPT终局综合审计，不自行PR/合并/部署。

## 基线刷新卡（每次派发都填，不能预写）

任务ID / 用户设计批准引用 / 真实origin/main完整SHA / 前序合入证据 / 当前相关diff / 分支名 / 允许文件范围 / 验收命令 / 停止点。

D1–D5未填真实SHA以前都标记 `NOT DISPATCHABLE`。通过设计确认不授权跳过每票代码独立审计。
