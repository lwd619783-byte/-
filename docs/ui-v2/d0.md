# UI V2 D0：冻结基线、原范围与迁移边界

记录时点：2026-09-21T13:37:25Z。本文件是 UI V2 新增事实与验收输入，不回写历史审计。机器清单见 [migration-inventory.json](migration-inventory.json)；`wired: false` 表示 D0 尚未确认新版接线，不表示旧功能不存在。

## 基线与范围

- 本地核对 `HEAD = origin/main = a029b1e3b96f8b8d28ec123cd741eadc09c12e3d`，分支 `codex/ui-v2-clear-research-workspace`；D0 读取时工作树干净。
- 原 Slice 2.5 交付 `675fb43b65c5f08122efdcfd6548c45ac1fbc8b0` 与上述 main 的 tree 相同；主任务已核验 PR #72 于 2026-09-21T13:10:34Z squash 合入。本任务无需依赖未合入父分支，不自行 PR / merge / 修改 main / 部署 Production。
- 追加只读核验于 `2026-09-21T13:46:17Z`：`gh pr view 72` 返回 [PR #72](https://github.com/lwd619783-byte/-/pull/72) `MERGED`，head `675fb43b65c5f08122efdcfd6548c45ac1fbc8b0`、merge `a029b1e3b96f8b8d28ec123cd741eadc09c12e3d`、合入时间如上；两提交 tree 均 `a4d9cf145ae28fe44850a0f1d0f44374b115c339`。[main CI 35603915441](https://github.com/lwd619783-byte/-/actions/runs/35603915441) 对 exact main SHA 的 push run 于 `2026-09-21T13:10:36Z` 创建，当前 `completed/success`。因此旧 CURRENT 的“未合入 / Hosted CI NOT_RUN”在代码交付状态上已过期；这不证明真实人工审核完成，也不替代对 UI V2 新差异的独立审查。本次未核实 Production 部署，不写 Production READY。
- `47612e1534e855561467043447e0ff6aa02b9f90` 是历史定向复审定位；`812e7551e67b0b8af4f673524e2578ecf2e19335` 是历史 Slice 2 基线，都不是当前 main。
- 实现栈取自 package.json：React 18、TypeScript 5、Vite 6；正文使用精确版本 react-markdown 10.1.0 / remark-gfm 4.0.1。不得将 HTML 原型当作生产代码、事实或迁移指令。
- 本轮明确以五工作入口、单一浅色、统一阅读取代 UI V1 的三皮肤产品策略。旧 `docs/ui-redesign/v1/01-design-contract.md` / `03-theme-and-charts.md` 保留历史事实；其多皮肤、默认 neon、主题切换验收不再约束 UI V2。数据、来源、图表身份和交互保护继续保留。

## 原 Slice 2.5 结账：没有完整真实 E2E PASS

现有 `stage-4-3-slice-2-5-preview-cutover.md` 顶部曾将真实 UPDATE/history/revoke 延期到 Knowledge V2；本次用户明确将这些保留在原验收范围。新的范围决定以本文件及 UI V2 CURRENT 同步为准，旧文档与旧报告不改写。

| 原范围项 | 历史证据 | D0 当前结论 |
| --- | --- | --- |
| 真实 ChatGPT 首轮读取、8-source CREATE | 用户确认；正式贡献文件及 importer 校验 | 历史 PASS，不冒充当前会话独立 MCP 观察 |
| 光通信贡献正式导入、待审全文 | 2026-09-21T12:24:30.904Z：8 refs / 8 extractions / 55 atoms / 3 conflicts / 1 CREATE / 6章 | 历史 IMPORTED / PENDING；当前存储未连接检查，NOT_VERIFIED |
| 原件与 reload | 同时点10份原件摘要一致，reload storage identical | 历史 PASS；UI V2 当前运行时 NOT_VERIFIED |
| 本人接受或拒绝 | 同时点 WikiEntry / WikiRevision / WikiReview / disposition 全部0 | PENDING / 当前 NOT_VERIFIED；看过页面不是接受 |
| 同一 Wiki 真实 UPDATE、完整历史与 reload | 旧报告 DEFERRED_NOT_ATTEMPTED | 原范围 PENDING / NOT_VERIFIED |
| 真实陈旧 base / 重复操作拒绝 | 既有单元与合成浏览器门禁，不等于真实本人流程 | 当前真实 run NOT_VERIFIED |
| 接受 UPDATE 后真实 ChatGPT 撤销拒读资料及知识 | 旧报告 DEFERRED_NOT_ATTEMPTED | 原范围 PENDING / NOT_VERIFIED |

不能为了结账替用户接受不满意的研究文章；后续真实 UPDATE 依赖用户真实决定及具备授权工具的会话。独立 UI 与合成回归可继续。Stage 4.3 整体仍 CURRENT，不能写 CLOSED。

### 三组运行时必须分别记账

1. 真实 run `optical-public-20260921` 历史观测：runtime `8aa28245556163f67b4beeb9233e278aa29da5bc`，deployment `dpl_6j9pLGnctUG1w3eaZtT56qw5Vc2y`，exact origin `https://investment-research-dashboard-oy854gw7y-lkdmkl.vercel.app`；batch `batch-cc5d6826-3e8c-42c5-924c-342d4255712e`。专用 profile 按旧 run 绑定，历史记录显示曾改用 Chrome；本轮没有启动或复制其 profile，当前存在性、授权及实际浏览器未重新核实。
2. 原分支最终代码 Preview 历史观测：runtime `675fb43b65c5f08122efdcfd6548c45ac1fbc8b0`，deployment `dpl_DHFnGNDp3zFMPs1PxMuY8k3AUQqi`，exact origin `https://investment-research-dashboard-dvr3fjlax-lkdmkl.vercel.app`，报告时间 `2026-09-21T12:55:44.491Z`。17项 remote synthetic smoke、平台保护302、READY 只属于这个历史时点和 runtime；不是 UI V2 通过。
3. UI V2 的 build、测试、deployment、origin、profile、testedAt 和输入类型必须另记；文档 HEAD 与被测 runtime SHA 分开。新 origin 看不到旧 IndexedDB 是隔离，不是删除也不是迁移成功。

用户提供 stage 到期为 `2026-09-22T05:49:25.359Z`，晚于 D0 读取时间；这仅说明名义时限尚未过去，不证明 stage active、授权有效或未被撤销。本轮不恢复旧 stage，不尝试认证，不猜测结果。

本地历史证据位置：`data-cache/slice25-closeout/final-675fb43/final-report.json`、`preview-verification.json`、`browser-matrix.json`。这些文件只读，不提交原件、贡献正文或登录信息。仓库脚本 `research-bridge-public-acceptance.mjs --action inspect` 虽然业务存储只读，仍启动 persistent Edge、打开 UI 并重写 run manifest；不能当作无副作用文件读取，也不能未经核实用于已改 Chrome 的 profile。本次未执行。

追加定位仅限该run目录：真实 manifest 为 gitignored `.local/acceptance-optical-20260921/run.json`，贡献为该目录 `inbox/round-1-create.json`（222594 bytes、SHA-256 `ba26fcd11208aa8ce929a10e2d826bbb854699b58e3a5a6703f547354e62e167`，本轮文件读取重新核对一致）。贡献是一个 CREATE，原 `document.bodyMarkdown` 37254字符、六章，可直接供隔离只读渲染测试，不能导入合成profile后冒充真实验收。manifest 当前文件中的 `humanDecision=pending` 记录于旧检查，`remoteStatus=readable` 的 `checkedAt=2026-09-21T11:05:23.143Z` 仍是历史状态；文件中的 `SLICE25_CLOSED_PENDING_INDEPENDENT_AUDIT` 标签不覆盖本次明确保留的原验收缺口。

同目录存在 `observe-create-readonly.mjs`：固定Chrome profile与origin、仅同源GET/HEAD、IDB readonly、localStorage只读、reload前后canonical比较；但会覆盖旧 `final-create-observation.json` 并留下浏览器窗口，报告还带旧 deferred 口径。`chrome-resume.mjs` 动态适配旧runner，inspect仍会更新manifest。两者本轮均未执行，也未打开profile内部文件或cookies。新的观测应保留旧报告、单独记录新时间与权限结果，不继承旧状态。

## CURRENT 路线核对

出处：`investment-dashboard-v2-financial-research-os-rebaseline-2026-09-11.md` §10，及 `development-execution-plan-2026-09-07.md` 当前主线段。

| 阶段 | 仓库实际名称与范围 | 本轮 UI 契约 |
| --- | --- | --- |
| 4.3 CURRENT | Research Memory & Thesis Compiler V1；六个主 Slice 加2.5；3 Creator→Wiki、4 Verified Claim、5 Thesis、6 Investment Expression 仍后续 | 同一知识底座；不因导航完成而升级领域能力 |
| 4.4 | Portfolio Exposure MVP；复用 Phase1B Local Core、thesis↔position、macro/industry exposure、target allocation、rebalance、独立方法准入 | `#/portfolio` 明确未连接；无虚假持仓/收益/暴露 |
| 4.5 | roadmap 标题 Research Bridge / Controlled Tool Layer；execution-plan 称 Research MCP Gateway / Controlled Tool Layer | `#/sources` 复用已提前实现的 selected private staging + OAuth read-only；不声称具备写工具或自动提审 |
| 4.6+ | Research Agent / Artifact / Global Coverage；Agent、Artifact、Notification、global、valuation 等，依赖 F1/F2/F3 与4.5 | 任务入口只接现有人工待办；无执行器不模拟研究运行 |

用户历史方向与仓库没有需要改序的冲突，但4.5名称、4.6+更广范围必须保留准确出处。本轮只在当前切片之后插入前端跨模块增强，不重新定义阶段，也不创建 Agent 专用 Wiki / Claim。

## 权威存储与保护

| owner / 存储 | 内容 | 既有保护与备份边界 |
| --- | --- | --- |
| WatchlistRepository / localStorage `investment-research-dashboard.watchlist.v2` | 观察、复盘、提醒状态、设置 | PersistedBaseGuard；JSON备份/导入/预检/确认；保留损坏原文 |
| EarningsExpectationRepository / localStorage `investment-research-dashboard.earnings-expectation.v1` | 本地预期快照、修正、导入历史、设置 | JSON/CSV导入导出；官方只读；原子校验不改 |
| CreatorViewpointRepository / localStorage `investment-research-dashboard.creator-viewpoint.v1` | Creator/source/observation/review/event | append-only、pre-import/pre-recovery备份、JSON恢复、XLSX分析副本 |
| BrowserWikiRepository / localStorage `investment-research-dashboard.wiki.v1` | Entry / Revision / Review | append-only、PersistedBaseGuard、pre-import/pre-recovery、完整Wiki JSON；**不含 IndexedDB 原件** |
| IndexedDbBrowserSourceRepository / IndexedDB `investment-research-dashboard.knowledge-ingestion.v1` | `state/current`内批次/资料元数据/贡献/处置；`originals`内原字节 | 同事务保存、generation CAS、逐原件SHA核验；**没有完整原件+状态的跨origin备份恢复API** |
| BridgeStagingPanel / localStorage `research-bridge.stage.v1:{batchId}` | 仅 stageId 提示 | 不是认证凭据；实际权限、期限与撤销由已有服务门禁决定 |
| 临时 private Blob / Research Bridge | 用户选择后的副本、认证受控读取 | 不是 Wiki/source authority，不当作可恢复正式备份 |
| Local Core SQLite | 既有 Node-only正式资产/审计基础 | 不由本次浏览器页面导入或复制，不新建浏览器账本 |

不清整个键空间，不导出 cookies、不复制个人登录态。UI主题改变只涉及展示偏好，不改业务存储。当前没有全量跨origin迁移能力，本轮策略是原真实资料留在原专用origin/profile，新的Preview使用隔离合成profile；真实资料迁移保持 NOT_IMPLEMENTED，不能以Wiki导出冒充完整备份。

回滚使用上一已核验前端 runtime / 普通后续修复提交，保留原origin/profile及同一业务owner/ID；不 reset、不强制推送、不回滚知识数据版本。若原origin被切换，先明确上述备份缺口，不清理或接管旧存储。

## 迁移与回归口径

旧 hash 路由全部保留：home、macro、industry、stocks、watchlist、verification、expectations、creators、memory、company/:id/:tab。行业 industry/segment、事件 event、公司 from 与标签、历史返回必须继续指向同一对象。memory旧页内四视图继续存在；新增 view 深链不能改变旧正文或owner。详细用户动作按机器清单逐项核对。

D0 本身未执行 UI V2 runtime 测试。既有脚本入口以 package.json 为准：env:check、test、build、contracts:validate、test:contracts、test:discovery、research:eval:check、test:research-eval、test:knowledge、test:bridge、test:bridge:browser；既有浏览器脚本 `wiki-browser-check.mjs` / `knowledge-ingestion-browser-check.mjs`。全站迁移还需导航、对象返回、320/390/768/1024/1440及大屏、键盘/缩放/reduced-motion、拒绝路径、性能同环境对比。合成输入通过与本人知识审核分列；不把旧测试数复制到新结论。

## D0 后续只读证据与原稿兼容预检

主任务追加 `data-cache/ui-v2/d0-real-observation.json`：`2026-09-21T13:48:01.178Z` 在原专用Chrome profile、原 exact origin/runtime/deployment 只读观测，**真实决定仍为 pending**。10 sources / 8 refs、0 WikiEntry / Revision / Review / disposition，贡献canonical与原文件一致、10原件SHA一致、reload相同、manifest未改；观测者未执行本人审核或业务写入。这更新了此前 D0 的“当前未核实”状态，但不补足 UPDATE/history/撤销拒读的真实验收，也没有刷新stage远端授权状态。

原稿只读兼容预检于 `2026-09-21T13:52:06.419Z–13:52:12.404Z` 完成：gitignored `data-cache/ui-v2/real-reader/` 内可信harness直接复用生产 `KnowledgeDocument`，从原贡献文件提取原title/summary/bodyMarkdown；不导入业务库、不审核、不执行原稿HTML。浏览器为临时Chrome context，origin `http://127.0.0.1:4192`，`runtimeSha=null`、`deploymentId=null`，明确是工作树Vite预检，不以当前HEAD冒充已提交运行时。

- 1440 / 390 / 320 CSS px，以及640px窗口下CSS `zoom:2`（320px等效重排）共55项通过。CSS zoom是补充重排检查，不冒充完整浏览器缩放或WCAG认证。
- 六章与全部788个 Markdown text/code leaf 在网页保留；原正文37254字符、SHA `e0959db52963acc5155661656c5b31cc1b9d22ee46e2a86ff5ec418e788c982e`；原文件字节前后相同。
- 原文全部4个去重ISO日期、16个去重64位摘要与 `parsedTextSha` 标签保留。目录键盘Enter正确聚焦六个原始h2；宽度未撑出整页。
- 正文无script/img/iframe/object/embed；零外部请求、零浏览器异常，localStorage和IndexedDB均未创建业务存储。原稿没有HTML节点，因此恶意HTML拒绝仍应由另列对抗fixture验证。
- `report.json`及8张截图均保留上述gitignored目录，已抽看320px开头与200%来源段；原稿全文、摘录及截图不加入公开仓库。该结果仅证明旧正文的安全阅读兼容，不证明真实人工接受或真实UPDATE闭环。
